#include <iostream>
#define circle(n) for(int i = 0; i < n; i ++)
using namespace std;

const int N = 120;
int s1, s2, s3, s4;
int a[N], b[N], c[N], d[N];

int main() {
	
	cin>>s1>>s2>>s3>>s4;
	circle(s1) cin>>a[i];
	circle(s2) cin>>b[i];
	circle(s3) cin>>c[i];
	circle(s4) cin>>d[i];
	
	
	circle(s4) cout<<d[i]<<" ";

	return 0;
}
